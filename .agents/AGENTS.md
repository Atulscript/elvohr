# Workspace Rules

- **Local Verification First**: Always test and verify code modifications locally (by running the development server and using the browser subagent to check `localhost`) before staging, committing, or deploying the changes live to `elvohr.com`.
